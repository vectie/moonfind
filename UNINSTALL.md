# Moonfind uninstall contract

Preserve all user-owned `raw/**`, `records/**`, `runs/**`, review receipts, paper metadata, accepted model ideas, experiment outcomes, and Bookkeeper handoffs. The installer may remove only regenerated caches, temporary files, application bundles, and pack-owned executable code. An upgrade or uninstall must not delete or rewrite accepted evidence.

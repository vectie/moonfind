{
  "name": "@vectie/moonfind-rabbita",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "moon build --target js --release",
    "dev": "vite",
    "prebuild": "moon build --target js --release",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": { "vite": "^6.1.0" }
}
